# Postgres connection string (Vercel Postgres / Neon).
# In Vercel, add the Neon integration and the platform injects POSTGRES_URL automatically.
# For local dev, point this at any Postgres 13+ database.
DATABASE_URL=postgres://user:password@host/dbname?sslmode=require

# Anthropic Claude API key — required by the analysis pipeline.
ANTHROPIC_API_KEY=

# Optional: shared secret for /api/admin/* ops endpoints.
# When set, admin routes require `Authorization: Bearer <ADMIN_TOKEN>`.
# When unset, /api/admin/analyze/[id] only triggers stuck rows (status=processing,
# analysis_started_at IS NULL, created_at older than 5 min) — safe bootstrap mode.
ADMIN_TOKEN=

# --- Telegram bridge (IDEAA-27) ---
# Token from @BotFather. Required for the bot to send/receive messages.
TELEGRAM_BOT_TOKEN=
# Random secret registered with setWebhook; Telegram echoes it in the
# X-Telegram-Bot-Api-Secret-Token header so we can reject spoofed webhooks.
TELEGRAM_WEBHOOK_SECRET=
# Comma-separated list of allowed Telegram user IDs (numeric). Anything else
# is dropped silently. Initial value: just Jan.
TELEGRAM_ALLOWED_USER_IDS=
# Public URL of this Next.js app. Defaults to https://$VERCEL_URL when unset.
# Used by /api/telegram/admin op=register-webhook.
TELEGRAM_PUBLIC_APP_URL=

# --- Paperclip API (used by the Telegram bridge) ---
# Base URL of the Paperclip API.
PAPERCLIP_API_URL=
# Long-lived service-token API key for the bot worker.
PAPERCLIP_BOT_API_KEY=
# Paperclip company id.
PAPERCLIP_COMPANY_ID=
# UI prefix for issue links rendered in Telegram messages (e.g. "IDEAA").
PAPERCLIP_UI_PREFIX=IDEAA

# --- Voice transcription (Whisper) ---
# Required for /api/telegram/webhook to transcribe Telegram voice messages.
# Without this set, voice messages get a friendly "not configured" reply.
OPENAI_API_KEY=

# --- Stripe (IDEAA-72 Phase A asymmetric €1.99 paywall) ---
# Stripe Payment Link URL for the €1.99 full-report unlock. Required for the
# paywall to be active; without it the card renders a disabled "Setup pending"
# state. Configure the link's after_completion.redirect.url to:
#   https://<host>/api/unlock/success?session_id={CHECKOUT_SESSION_ID}
# so buyers land back on our domain and the unlock cookie can be minted.
STRIPE_PAYMENT_LINK_UNLOCK=
# Webhook signing secret from Stripe (Developers → Webhooks → endpoint).
# Required for /api/stripe/webhook to verify event signatures.
STRIPE_WEBHOOK_SECRET=
# Telegram chat id to ping on purchases. Defaults to the first entry of
# TELEGRAM_ALLOWED_USER_IDS when unset.
IDEAA_NOTIFY_TELEGRAM_CHAT_ID=
# Email shown as a mailto fallback when a Stripe link is not configured.
IDEAA_FALLBACK_CONTACT_EMAIL=

# --- /new command defaults ---
# Goal to attach new issues to when the user runs /new <title>.
# If unset, /new falls back to the goal of the last issue tagged in chat.
TELEGRAM_NEW_DEFAULT_GOAL_ID=
# Agent to assign new /new issues to. If unset, the issue is created
# unassigned and the manager/board will route it.
TELEGRAM_NEW_DEFAULT_ASSIGNEE_AGENT_ID=
